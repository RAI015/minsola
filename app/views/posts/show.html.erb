<% provide :title, set_address(@post.prefecture.name, @post.city.name) %>

<%= render "shared/success_messages" %>

<div class="container pt-4 pb-3">
  <div class="row">
    <div class="col-md-6">
      <%= render @post %>
    </div>
    <div class="col-md-6">
      <%= render partial: 'show_comments_likes', locals: { post: @post, comment: @comment } %>
    </div>
  </div>
</div>